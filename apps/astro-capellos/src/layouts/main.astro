---
import '../styles/global.css';
import { ViewTransitions } from 'astro:transitions';
import SidebarNav from '../components/SidebarNav.astro';
import {getNavSettings} from '../queries/settings';

interface Props {
	title?: string;
}

const { title = 'Capellos' } = Astro.props;
const navSettings = await getNavSettings();
---

<html lang="en">
	<head>
		<meta charset="utf-8" />
		<meta name="viewport" content="width=device-width" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<title>{title}</title>
		<ViewTransitions />
	</head>
	<body>
		<SidebarNav triggerBg={navSettings.triggerBackgroundColor} triggerIcon={navSettings.triggerIconColor} />
		<slot />
	</body>
</html>
